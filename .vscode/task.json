{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "ROS package build",
            "type": "shell",
            "command": "colcon build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
